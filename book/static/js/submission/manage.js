var form = document.getElementById('submitForm'); // Replace 'your-form-id' with the actual ID of your form

var submitButton = document.getElementById('submitOffenseBtn'); // Replace 'your-submit-button-id' with the actual ID of your button

submitButton.addEventListener('click', function(event) {
    event.preventDefault();
});
